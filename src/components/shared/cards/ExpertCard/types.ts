import { TImageType } from "@/types";

export type TProps = {
    name: string;
    image: TImageType;
    jobTitle: string;
    instagramLink: string;
    linkedInLink: string;
    facebookLink: string;
};
