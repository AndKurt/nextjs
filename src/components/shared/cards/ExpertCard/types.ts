import { StaticImageData } from "next/image";

export type TProps = {
    name: string;
    image: StaticImageData;
    jobTitle: string;
    instagramLink: string;
    linkedInLink: string;
    facebookLink: string;
};
